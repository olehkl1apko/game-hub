import {
  IPlatform,
  IGame,
  IGameQuery,
  IGenre,
  ISortOrders,
} from "./interfaces";
import { sortOrders, countSkeletons, iconMap } from "./variables";

export type { IPlatform, IGame, IGameQuery, IGenre, ISortOrders };
export { sortOrders, countSkeletons, iconMap };
